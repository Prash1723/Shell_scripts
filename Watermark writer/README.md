# Watermark for images

**Aim**: Bash script to add watermark in an image with one command.

**Methodology**: Basically the script finds whatever images are saved in your desired folder location. Then adds in watermark in all 4 corners + in the middle.

